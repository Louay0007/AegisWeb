global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: aegisweb-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - api:3001

rule_files:
  - /etc/prometheus/alerts.yml
